@model Pie

@*<h2>@Model.Name</h2>*@

@*<div class="row">
    <div class="col-md-12">
        <img class="img-fluid" alt="@Model.Name" src="@Model.ImageUrl">
        <div class="caption-full">
            <h3 class="pull-right">$ @Model.Price</h3>
            <h3>
                <a href="#">@Model.Name</a>
            </h3>
            <h4>@Model.ShortDescription</h4>
            <p>@Model.LongDescription</p>
        </div>
    </div>
</div>*@

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <img class="card-img-top" src="@Model.ImageUrl" alt="@Model.Name">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <h4 class="card-title">
                            <a href="#" class="text-right">@Model.Name</a>
                        </h4>
                    </div>
                    <div class="col-md-6 text-right">
                        <h5 class="card-title">$ @Model.Price.ToString()</h5>
                    </div>
                </div>

                <div class="card-text">
                    <h5>
                        @Model.ShortDescription
                    </h5>
                    <p>
                        @Model.LongDescription
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>